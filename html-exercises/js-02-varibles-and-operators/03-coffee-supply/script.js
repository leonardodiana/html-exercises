let myAge = 28;
let maxAge = 100;
let coffeeADay = 3;
let coffeeForLife = (coffeeADay*365) * (maxAge-myAge);
console.log("You will need " + coffeeForLife + " cups of coffee to last you until the ripe old age of " + maxAge);